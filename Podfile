# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AlertMe' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AlertMe
  pod 'RealmSwift'
  pod 'Alamofire', '~> 4.0'
  pod 'PromiseKit', '~> 4.0'
  pod 'SwiftyJSON', '3.0.0'

  target 'AlertMeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AlertMeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end